function pow(x: score, y: score) -> score:
    i: score = 0;
    result: score = 1
    while i < y:
        result *= x
        i += 1
    return result


for x: score in 1 .. 10:
    for y: score in 1 .. 5:
        print(f"{x} в степени {y} равняется {pow(x, y)}")
